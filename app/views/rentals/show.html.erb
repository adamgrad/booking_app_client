<div class="container">
  <h1>Rental</h1>
  <p>
    <%= @rental.name %>  -  <%= @rental.daily_rate %>
    <%= link_to "Edit", edit_rental_path(@rental), class: "btn btn-primary btn-xs" %>
  </p>
</div>